# File: packages/api/src/mcp/types/index.ts

# 1. Purpose
**File Type:** TS (TypeScript type definitions)

**What this file represents:**
This file is a typescript type definitions located at `packages/api/src/mcp/types/index.ts`.

**Documentation:** as t from '@modelcontextprotocol/sdk/types.js';

**Primary exports:** 29 exported element(s)
- StdioOptions
- WebSocketOptions
- SSEOptions

**File size:** 4,424 bytes


# 2. Domain Role
**Domain:** Model Context Protocol Integration

**Business relevance:**
This file is part of the Model Context Protocol Integration domain within the LibreChat application.



# 3. Public API (FULL DETAIL)
### Exported Functions

- `StdioOptions()` — named export
- `WebSocketOptions()` — named export
- `SSEOptions()` — named export
- `StreamableHTTPOptions()` — named export
- `MCPOptions()` — named export
- `MCPServers()` — named export
- `MCPResource()` — named export
- `LCTool()` — named export
- `LCFunctionTool()` — named export
- `LCAvailableTools()` — named export
- `LCManifestTool()` — named export
- `LCToolManifest()` — named export
- `MCPPrompt()` — named export
- `ConnectionState()` — named export
- `MCPTool()` — named export
- `MCPToolListResponse()` — named export
- `ToolContentPart()` — named export
- `ImageContent()` — named export
- `MCPToolCallResponse()` — named export
- `Provider()` — named export
- `FormattedContent()` — named export
- `FileSearchSource()` — named export
- `Artifacts()` — named export
- `FormattedContentResult()` — named export
- `ImageFormatter()` — named export
- `FormattedToolResponse()` — named export
- `ParsedServerConfig()` — named export
- `BasicConnectionOptions()` — named export
- `OAuthConnectionOptions()` — named export



# 4. Internal Structure
*No significant internal structure detected.*


# 5. Internal Behavior & Data Flow
### Data Flow Steps

1. **Input:** Function parameters
2. **Transformations:** Data processing logic
3. **External calls:** Dependent modules
4. **Output:** Return value or side effects


# 6. Relationships & Collaboration
### Imported Dependencies (2)

**NPM Packages:**
- `zod`
- `librechat-data-provider`



# 7. Database Interaction Mapping
**Database:** MongoDB via Mongoose



# 8. UI Behavior (if applicable)
**No UI behavior.** (Not a UI component)


# 9. Key Logic Snippets
*No significant logic snippets extracted. See full source file for implementation details.*


# 10. Architectural Concerns
**Security:** Handles sensitive data (passwords, tokens, authentication)
**Async Behavior:** Asynchronous operations present


# 11. Migration Mapping (Legacy → Modern)
### Target Location in Modern System

**Backend:** Backend code


# 12. Migration Concerns & Recommendations
*No major migration concerns identified. Standard migration process should apply.*


# 13. Dependencies
### dependsOn (2)

- `zod`
- `librechat-data-provider`



# 14. Tags
```
- typescript
- mcp-integration
- application-code
- librechat
- source-file
```

