# File: packages/data-provider/src/types/agents.ts

# 1. Purpose
**File Type:** TS (TypeScript type definitions)

**What this file represents:**
This file is a typescript type definitions located at `packages/data-provider/src/types/agents.ts`.

**Documentation:** eslint-disable @typescript-eslint/no-namespace */

**Primary exports:** 43 exported element(s)
- MessageType
- ImageDetail
- ReasoningContentText

**File size:** 10,890 bytes


# 2. Domain Role
**Domain:** Agent Orchestration & Configuration

**Business relevance:**
This file is part of the Agent Orchestration & Configuration domain within the LibreChat application.



# 3. Public API (FULL DETAIL)
### Exported Functions

- `MessageType()` — named export
- `ImageDetail()` — named export
- `ReasoningContentText()` — named export
- `MessageContentText()` — named export
- `AgentUpdate()` — named export
- `MessageContentImageUrl()` — named export
- `MessageContentComplex()` — named export
- `MessageContent()` — named export
- `ToolCall()` — named export
- `ToolEndEvent()` — named export
- `ToolCallContent()` — named export
- `ToolCallChunk()` — named export
- `EventName()` — named export
- `RunStep()` — named export
- `RunStepDeltaEvent()` — named export
- `StepDetails()` — named export
- `MessageCreationDetails()` — named export
- `ToolCallsDetails()` — named export
- `ToolCallDelta()` — named export
- `AgentToolCall()` — named export
- `ExtendedMessageContent()` — named export
- `MessageDeltaEvent()` — named export
- `MessageDelta()` — named export
- `ReasoningDeltaEvent()` — named export
- `ReasoningDelta()` — named export
- `ReasoningDeltaUpdate()` — named export
- `ContentType()` — named export
- `ToolCallResult()` — named export
- `AuthTypeEnum()` — named export
- `AuthorizationTypeEnum()` — named export
- `TokenExchangeMethodEnum()` — named export
- `Action()` — named export
- `ActionMetadata()` — named export
- `ActionAuth()` — named export
- `ActionMetadataRuntime()` — named export
- `MCP()` — named export
- `MCPMetadata()` — named export
- `MCPAuth()` — named export
- `AgentToolType()` — named export
- `ToolMetadata()` — named export
- `BaseMessage()` — named export
- `BaseGraphState()` — named export
- `GraphEdge()` — named export



# 4. Internal Structure
*No significant internal structure detected.*


# 5. Internal Behavior & Data Flow
### Data Flow Steps

1. **Input:** Function parameters
2. **Transformations:** Data processing logic
3. **External calls:** Dependent modules
4. **Output:** Return value or side effects


# 6. Relationships & Collaboration
### Imported Dependencies (1)

**Relative Imports:**
- `./runs`



# 7. Database Interaction Mapping
**No direct database interaction.**


# 8. UI Behavior (if applicable)
**No UI behavior.** (Not a UI component)


# 9. Key Logic Snippets
*No significant logic snippets extracted. See full source file for implementation details.*


# 10. Architectural Concerns
**Security:** Handles sensitive data (passwords, tokens, authentication)


# 11. Migration Mapping (Legacy → Modern)
### Target Location in Modern System

**Shared:** Shared package code


# 12. Migration Concerns & Recommendations
- Contains complex conditional logic that may need review


# 13. Dependencies
*No dependency information available.*


# 14. Tags
```
- typescript
- agent-orchestration
- application-code
- librechat
- source-file
```

